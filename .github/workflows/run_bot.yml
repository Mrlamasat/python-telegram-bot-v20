name: Run Telegram Bot

on:
  workflow_dispatch:  # يظهر زر التشغيل اليدوي

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyrogram asyncpg TgCrypto psycopg2-binary

      - name: Run bot
        env:
          SESSION_STRING: ${{ secrets.SESSION_STRING }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          ADMIN_CHANNEL: ${{ secrets.ADMIN_CHANNEL }}
          PUBLIC_CHANNELS: ${{ secrets.PUBLIC_CHANNELS }}
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          API_ID: ${{ secrets.API_ID }}
          API_HASH: ${{ secrets.API_HASH }}
          SOURCE_CHANNEL: "@Ramadan4kTV"  # يمكن تغييره إذا أردت
        run: |
          # حلقة لإعادة تشغيل البوت تلقائيًا عند أي خطأ
          while true; do
            echo "🚀 تشغيل البوت..."
            python bot.py
            echo "⚠️ البوت توقف. سيتم إعادة التشغيل خلال 5 ثوانٍ..."
            sleep 5
          done
